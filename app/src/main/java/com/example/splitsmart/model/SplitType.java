package com.example.splitsmart.model;

/**
 * The type of split for a user's share.
 */
public enum SplitType {
    DEFAULT, // Split equally
    FIXED_AMOUNT, // Split a specific amount in dollars
    PERCENTAGE // Split a specific percentage of the total
};
